# 智慧养老系统 - CentOS 7 部署更新指南

## 📋 文档说明

**适用场景：** 更新虚拟机CentOS 7上已部署的智慧养老系统
**更新内容：**
- ✅ 品牌Logo更新（"中州养老" → "智慧养老"）
- ✅ 登录页面优化（移除默认密码）
- ✅ API接口文档更新
- ✅ 代码bug修复

**更新日期：** 2026-03-02
**部署方式：** Docker Compose

---

## 🏗️ 当前部署架构

### 服务器信息
- **操作系统：** CentOS 7
- **IP地址：** 192.168.100.128（根据实际情况调整）
- **部署路径：** `/opt/zzyl`

### 服务组件
| 服务 | 容器名称 | 端口 | 说明 |
|------|---------|------|------|
| 前端（Nginx） | zzyl-frontend | 80, 443 | Vue 3应用 |
| 后端（Spring Boot） | zzyl-backend | 9995 | Java 11应用 |
| MySQL | zzyl-mysql | 3306 | 数据库 8.0.19 |
| Redis | zzyl-redis | 6379 | 缓存服务 |

### 访问地址
- **前端地址：** http://192.168.100.128
- **API文档：** http://192.168.100.128:9995/doc.html

---

## 🚀 更新部署步骤

### 方式一：完整更新（推荐）

适用于：代码有较大改动，需要重新构建镜像

#### 步骤1：备份当前数据

```bash
# 登录到虚拟机
ssh root@192.168.100.128

# 进入项目目录
cd /opt/zzyl

# 备份数据库
docker exec zzyl-mysql mysqldump -uroot -p123456 zzyl > backup_$(date +%Y%m%d_%H%M%S).sql

# 备份上传的文件（如果有）
tar -czf uploads_backup_$(date +%Y%m%d).tar.gz data/uploads/

# 备份配置文件
tar -czf config_backup_$(date +%Y%m%d).tar.gz docker/
```

#### 步骤2：拉取最新代码

```bash
# 进入项目目录
cd /opt/zzyl

# 暂存本地配置修改（如果有）
git stash

# 拉取最新代码
git pull origin main

# 恢复本地配置（如果之前有暂存）
# git stash pop
```

#### 步骤3：更新配置文件

检查并更新以下配置文件：

**3.1 后端配置（application-docker.yml）**

位置：`zzyl/zzyl-web/src/main/resources/application-docker.yml`

确保以下配置正确：
```yaml
spring:
  datasource:
    druid:
      url: jdbc:mysql://mysql:3306/zzyl?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
      username: root
      password: 123456

  redis:
    host: redis
    port: 6379
    database: 1
```

**3.2 Docker Compose配置（docker-compose.yml）**

确认配置无需修改，保持原样即可。

#### 步骤4：停止旧服务

```bash
cd /opt/zzyl

# 停止所有服务
docker compose down

# 或者使用管理脚本
./docker-manage.sh stop
```

#### 步骤5：重新构建镜像

```bash
cd /opt/zzyl

# 构建所有镜像（强制重新构建）
docker compose build --no-cache

# 或者只构建修改过的服务
docker compose build --no-cache backend frontend
```

**预计耗时：** 5-10分钟

#### 步骤6：启动新服务

```bash
cd /opt/zzyl

# 启动所有服务
docker compose up -d

# 或者使用管理脚本
./docker-manage.sh start
```

#### 步骤7：验证部署

```bash
# 查看服务状态
docker compose ps

# 查看后端日志
docker logs -f zzyl-backend --tail 100

# 查看前端日志
docker logs -f zzyl-frontend --tail 50
```

**检查点：**
- ✅ 所有容器状态为 `healthy`
- ✅ 后端日志无ERROR
- ✅ 数据库连接正常

#### 步骤8：功能测试

访问以下地址进行测试：

1. **前端登录页面：** http://192.168.100.128
   - ✅ 检查Logo是否更新为"智慧养老"
   - ✅ 检查登录框无默认密码

2. **API文档：** http://192.168.100.128:9995/doc.html
   - ✅ 可以正常访问
   - ✅ 接口列表正常显示

3. **登录测试：**
   - 用户名：`admin@qq.com`
   - 密码：`888itcast.CN764%...`
   - ✅ 能够正常登录

---

### 方式二：快速更新（仅更新代码）

适用于：只修改了代码，不需要重新构建基础镜像

#### 步骤1：更新前端静态文件

```bash
cd /opt/zzyl

# 进入前端项目目录
cd project-zzyl-admin-vue3-java

# 拉取最新代码
git pull origin main

# 重新构建
npm run build

# 复制到Nginx容器
docker cp dist/. zzyl-frontend:/usr/share/nginx/html/

# 重启前端容器
docker restart zzyl-frontend
```

#### 步骤2：更新后端应用

```bash
cd /opt/zzyl

# 进入后端项目目录
cd zzyl

# 拉取最新代码
git pull origin master

# 重新编译
mvn clean package -DskipTests

# 复制jar包到容器
docker cp zzyl-web/target/zzyl-web.jar zzyl-backend:/app/app.jar

# 重启后端容器
docker restart zzyl-backend
```

---

## 🔧 常见问题排查

### 问题1：容器启动失败

**症状：**
```bash
docker compose ps
# 显示容器状态为 Exit 1 或 Restarting
```

**解决方案：**
```bash
# 查看详细日志
docker compose logs backend

# 常见原因：
# 1. 数据库未就绪 - 等待MySQL完全启动
# 2. 端口占用 - 检查端口占用情况
netstat -tuln | grep 9995

# 3. 配置错误 - 检查配置文件
cat zzyl/zzyl-web/src/main/resources/application-docker.yml
```

### 问题2：前端无法访问后端API

**症状：**
- 前端页面能访问，但无法获取数据
- 浏览器控制台显示网络错误

**解决方案：**
```bash
# 1. 检查后端服务是否正常
curl http://localhost:9995/doc.html

# 2. 检查Nginx配置
docker exec zzyl-frontend cat /etc/nginx/conf.d/default.conf

# 3. 检查网络连接
docker network ls
docker network inspect zzyl-network
```

### 问题3：数据库连接失败

**症状：**
```
Communications link failure
```

**解决方案：**
```bash
# 1. 检查MySQL容器状态
docker ps | grep mysql

# 2. 检查MySQL日志
docker logs zzyl-mysql --tail 100

# 3. 手动测试连接
docker exec -it zzyl-mysql mysql -uroot -p123456

# 4. 检查数据库是否初始化
docker exec -it zzyl-mysql mysql -uroot -p123456 -e "SHOW DATABASES;"
```

### 问题4：Redis连接失败

**症状：**
```
Unable to connect to Redis
```

**解决方案：**
```bash
# 1. 检查Redis容器状态
docker ps | grep redis

# 2. 测试Redis连接
docker exec -it zzyl-redis redis-cli ping

# 3. 检查Redis配置
docker exec zzyl-redis cat /usr/local/etc/redis/redis.conf
```

---

## 📊 性能优化建议

### 1. JVM参数优化

编辑 `docker-compose.yml`，调整后端JVM参数：

```yaml
environment:
  JAVA_OPTS: >-
    -Xms1024m          # 初始堆内存（根据服务器配置调整）
    -Xmx2048m          # 最大堆内存（根据服务器配置调整）
    -XX:+UseG1GC       # 使用G1垃圾回收器
    -XX:MaxRAMPercentage=75.0
    -XX:+UseContainerSupport
```

### 2. MySQL优化

编辑 `docker/mysql/my.cnf`：

```ini
[mysqld]
# 缓冲池大小（建议设置为物理内存的70-80%）
innodb_buffer_pool_size = 1G

# 日志配置
innodb_log_file_size = 256M
innodb_log_buffer_size = 16M

# 连接数
max_connections = 500

# 查询缓存
query_cache_size = 0
query_cache_type = 0
```

### 3. Redis优化

编辑 `docker/redis/redis.conf`：

```conf
# 最大内存
maxmemory 512mb

# 内存策略
maxmemory-policy allkeys-lru

# 持久化
save 900 1
save 300 10
save 60 10000
```

---

## 🔐 安全加固

### 1. 修改默认密码

```bash
# 修改MySQL密码
docker exec -it zzyl-mysql mysql -uroot -p123456
ALTER USER 'root'@'%' IDENTIFIED BY '新密码';
FLUSH PRIVILEGES;

# 更新配置文件
vi zzyl/zzyl-web/src/main/resources/application-docker.yml
# 修改 SPRING_DATASOURCE_DRUID_PASSWORD

# 重启服务
docker compose restart backend
```

### 2. 配置防火墙

```bash
# 开放必要端口
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=443/tcp
firewall-cmd --permanent --add-port=9995/tcp

# 重载防火墙
firewall-cmd --reload
```

### 3. 配置HTTPS（可选）

```bash
# 1. 准备SSL证书
mkdir -p docker/nginx/ssl
cp your_certificate.crt docker/nginx/ssl/
cp your_private.key docker/nginx/ssl/

# 2. 修改Nginx配置
# 编辑 docker/nginx/default.conf，添加HTTPS配置

# 3. 重启前端服务
docker compose restart frontend
```

---

## 📝 日常维护

### 日志管理

```bash
# 查看实时日志
docker compose logs -f

# 查看最近100行日志
docker compose logs --tail 100

# 查看特定服务日志
docker logs zzyl-backend --tail 100 -f

# 清理日志（保留最近7天）
find logs/ -name "*.log" -mtime +7 -delete
```

### 数据备份

```bash
# 使用管理脚本备份
./docker-manage.sh backup

# 手动备份
docker exec zzyl-mysql mysqldump -uroot -p123456 zzyl > backup_$(date +%Y%m%d).sql

# 定时备份（crontab）
crontab -e
# 添加：每天凌晨2点备份
0 2 * * * cd /opt/zzyl && ./docker-manage.sh backup
```

### 服务监控

```bash
# 查看容器资源使用
docker stats

# 查看容器健康状态
docker compose ps

# 使用管理脚本
./docker-manage.sh status
```

---

## 🔄 回滚操作

如果更新后出现问题，可以快速回滚：

### 方式一：代码回滚

```bash
cd /opt/zzyl

# 查看提交历史
git log --oneline

# 回滚到指定版本
git reset --hard <commit_id>

# 重新构建
docker compose build --no-cache
docker compose up -d
```

### 方式二：数据回滚

```bash
cd /opt/zzyl

# 恢复数据库
./docker-manage.sh restore
# 选择要恢复的备份文件
```

---

## 📋 更新检查清单

更新完成后，请逐项检查：

- [ ] 拉取最新代码成功
- [ ] 数据库备份完成
- [ ] Docker镜像构建成功
- [ ] 所有容器启动正常（healthy状态）
- [ ] 前端页面可访问
- [ ] Logo已更新为"智慧养老"
- [ ] 登录功能正常
- [ ] API文档可访问
- [ ] 核心业务功能正常
- [ ] 日志无ERROR
- [ ] 性能正常

---

## 🆘 紧急联系

如遇到无法解决的问题：

1. 查看日志：`docker compose logs`
2. 检查配置：`cat docker-compose.yml`
3. 重启服务：`docker compose restart`
4. 恢复备份：`./docker-manage.sh restore`

---

## 📚 相关文档

- [Docker Compose官方文档](https://docs.docker.com/compose/)
- [CentOS 7 Docker安装指南](https://docs.docker.com/engine/install/centos/)
- [项目API接口文档](./API接口文档-完整版.md)
- [项目技术文档](./项目文档.md)

---

**智慧养老系统 - 部署更新指南** 🚀
**最后更新：2026-03-02**
